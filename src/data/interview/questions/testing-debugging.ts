import type { InterviewQuestion } from '@/types/interview';

export const testingDebuggingQuestions: InterviewQuestion[] = [
  {
    id: 'interview.testing.swift_testing_framework.001',
    question: 'What are the major advantages of the new Swift Testing framework (iOS 18+ / Swift 6) over legacy XCTest?',
    domainId: 'testing',
    moduleId: 'testing-unit-mod',
    topicId: 'testing-swift-testing-framework',
    difficulty: 'intermediate',
    type: 'comparison',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Testing', 'Swift Testing', 'XCTest', 'Swift 6'],
    modelAnswer: 'Swift Testing uses modern Swift language features: 1) `@Test` and `@Suite` attribute macros replace rigid `test...()` naming prefixes and `XCTestCase` class inheritance, allowing structs or actors to be suites. 2) Rich `#expect` and `#require` macros: `#expect` evaluates expressions with detailed AST macro failure diagnostics (displaying exact sub-expression values), while `#require` unwraps optionals or bails out immediately if false. 3) Parameterized testing: `@Test(arguments: [1, 2, 3])` runs matrix tests automatically without boilerplate for-loops. 4) Native async/await support with parallel test execution by default.',
    keyPoints: [
      '`@Test` on structs/actors replaces XCTestCase inheritance.',
      '`#expect` macro provides source-level diagnostic output on failures.',
      '`#require` stops execution on failure (replaces XCTUnwrap).',
      'Parameterized tests run test matrices natively.',
      'Parallel test execution by default.',
    ],
    commonMistakes: [
      'Using `try XCTUnwrap(...)` inside Swift Testing instead of `#require(...)`.',
    ],
    followUps: [
      {
        id: 'interview.testing.swift_testing_framework.001.f1',
        parentQuestionId: 'interview.testing.swift_testing_framework.001',
        question: 'How do you attach custom tags or bugs to tests in Swift Testing?',
        modelAnswer: 'Pass traits into the attribute: `@Test(.tags(.critical), .bug("PROJ-123", "User login issue"))`.',
        keyPoints: ['Traits system', 'Metadata tags and bug associations'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['testing-swift-testing-framework'],
  },
  {
    id: 'interview.testing.test_doubles_types.002',
    question: 'Distinguish between Dummy, Stub, Spy, Mock, and Fake in automated iOS testing.',
    domainId: 'testing',
    moduleId: 'testing-unit-mod',
    topicId: 'testing-test-doubles-mocking',
    difficulty: 'intermediate',
    type: 'comparison',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Testing', 'Test Doubles', 'Mocks', 'Stubs'],
    modelAnswer: '1) Dummy: Passed around but never actually used (e.g. filling a required parameter). 2) Stub: Provides canned answers to calls made during the test (e.g. returning pre-set sample users), usually not responding to anything outside what\'s programmed. 3) Spy: A stub that also records information about how it was called (e.g. `callCount`, `capturedArguments`). 4) Mock: Pre-programmed with expectations which form a specification of the calls they are expected to receive; fails the test if expectations aren\'t met. 5) Fake: Has a working implementation, but takes shortcuts not suitable for production (e.g. an in-memory SQLite or dictionary-backed repository).',
    keyPoints: [
      'Dummy: Filler object, never invoked.',
      'Stub: Provides hardcoded return values.',
      'Spy: Records call counts and parameters.',
      'Mock: Pre-programmed with expectations that assert automatically.',
      'Fake: Simplified working in-memory implementation.',
    ],
    commonMistakes: [
      'Calling every test double a "Mock" regardless of whether it actually performs behavior verification.',
    ],
    followUps: [
      {
        id: 'interview.testing.test_doubles_types.002.f1',
        parentQuestionId: 'interview.testing.test_doubles_types.002',
        question: 'Why are Spies often preferred over strict Mocks in Swift unit tests?',
        modelAnswer: 'Spies allow following the Arrange-Act-Assert pattern: you assert on `spy.recordedCalls` at the END of the test, making the test easier to read than setting up pre-test mock expectations.',
        keyPoints: ['Arrange-Act-Assert alignment', 'Assertion at test conclusion'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['testing-test-doubles-mocking', 'arch-di'],
  },
  {
    id: 'interview.debugging.lldb_workflow.003',
    question: 'Explain the difference between `po`, `p`, and `v` in LLDB, and how conditional breakpoints save time.',
    domainId: 'debugging',
    moduleId: 'debugging-tools-mod',
    topicId: 'debugging-lldb-mastery',
    difficulty: 'intermediate',
    type: 'practical',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Debugging', 'LLDB', 'Breakpoints', 'Xcode'],
    modelAnswer: '`po` (print object) compiles an expression and calls `description` / `debugDescription` on the result. `p` (print) compiles an expression, executes it, and formats the raw result without calling description. `v` (frame variable) does NOT compile any code: it reads variables directly from the stack frame and memory using metadata formatters (drastically faster than `po` and works even when compiler context is broken). Conditional breakpoints evaluate an expression (e.g. `user.id == 42`) and only pause execution when true, with optional debugger actions (sounds, logging text without halting, or calling LLDB commands).',
    keyPoints: [
      '`po` compiles expression and calls description.',
      '`p` compiles expression and inspects structure.',
      '`v` (frame variable) does zero compilation (instantaneous and reliable).',
      'Conditional breakpoints stop execution only on targeted conditions.',
      'Action breakpoints can log values without halting execution ("Automatically continue").',
    ],
    commonMistakes: [
      'Using `po` on heavy objects in tight loops, causing severe debugger lag from constant JIT compilation.',
    ],
    followUps: [
      {
        id: 'interview.debugging.lldb_workflow.003.f1',
        parentQuestionId: 'interview.debugging.lldb_workflow.003',
        question: 'What is a Symbolic Breakpoint and give a practical iOS example?',
        modelAnswer: 'A breakpoint on a symbol name rather than a file line. Example: `UIViewAlertForUnsatisfiableConstraints` halts execution the instant an Auto Layout constraint conflict occurs, revealing the conflicting view.',
        keyPoints: ['Symbol-based triggering', 'UIViewAlertForUnsatisfiableConstraints'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['debugging-lldb-mastery', 'uikit-autolayout-engine'],
  },
  {
    id: 'interview.debugging.dsym_symbolication.004',
    question: 'How does crash log symbolication work using dSYM files, and what does the `atos` command line tool do?',
    domainId: 'debugging',
    moduleId: 'debugging-tools-mod',
    topicId: 'debugging-crash-symbolication',
    difficulty: 'advanced',
    type: 'conceptual',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Debugging', 'Crash Logs', 'dSYM', 'atos', 'Symbolication'],
    modelAnswer: 'Release binary compilations strip function names and file line strings to save space and deter reverse engineering. The compiler outputs a Debug Symbol (`.dSYM`) package containing DWARF symbol tables that map raw memory offsets to original source file names, class names, and line numbers. When a crash occurs, raw memory addresses (e.g. `0x104b28f4c`) are recorded. `atos` (address to symbol) takes the binary architecture, the dSYM file, the load address of the binary, and the crash memory address, outputting the exact function and source line.',
    keyPoints: [
      'Release builds strip debug symbols into a separate `.dSYM` bundle.',
      'Symbolication maps raw hexadecimal memory offsets back to source file lines.',
      '`atos` command syntax: `atos -arch arm64 -o MyFramework.dSYM/... -l <loadAddress> <crashAddress>`.',
      'dSYM UUID must exactly match the crash log Binary Image UUID (checked via `dwarfdump --uuid`).',
    ],
    commonMistakes: [
      'Attempting to symbolicate a crash with a dSYM built from a different build or commit (UUID mismatch fails symbolication).',
    ],
    followUps: [
      {
        id: 'interview.debugging.dsym_symbolication.004.f1',
        parentQuestionId: 'interview.debugging.dsym_symbolication.004',
        question: 'What is ASLR (Address Space Layout Randomization) and why does it require the load address?',
        modelAnswer: 'ASLR randomizes the starting memory location of an app binary upon every launch for security. To calculate the static symbol offset, `atos` subtracts the randomized load address from the crash address.',
        keyPoints: ['ASLR security randomization', 'Load address offset calculation'],
        difficulty: 'expert',
      },
    ],
    relatedTopics: ['debugging-crash-symbolication'],
  },
  {
    id: 'interview.performance.time_profiler.005',
    question: 'How do you analyze main thread hitches and hangs using the Xcode Time Profiler Instrument?',
    domainId: 'performance',
    moduleId: 'performance-profiling-mod',
    topicId: 'performance-time-profiler',
    difficulty: 'advanced',
    type: 'practical',
    estimatedMinutes: 5,
    frequency: 'high',
    tags: ['Performance', 'Time Profiler', 'Instruments', 'Hangs'],
    modelAnswer: '1) Profile app with release configuration on a physical device. 2) Reproduce scrolling or UI interaction. 3) Inspect the "Hitch Time Ratio" (target is <5ms/s for smooth 60fps/120fps). 4) Select the hang region on the timeline and inspect the Main Thread call tree. 5) Check "Separate by Thread", "Invert Call Tree", and "Hide System Libraries". Inverting the call tree places the heaviest leaf functions at the top, showing which of your functions consumed the most CPU time. 6) Identify heavy operations (JSON parsing, regex, core data queries) and offload them to background tasks.',
    keyPoints: [
      'Profile on physical hardware in Release mode.',
      '"Hide System Libraries" filters noise; "Invert Call Tree" surfaces heaviest leaf methods.',
      'Check frame budget: 16.6ms for 60Hz; 8.3ms for 120Hz ProMotion.',
      'Hitch Time Ratio measures dropped frame severity.',
    ],
    commonMistakes: [
      'Profiling performance on the iOS Simulator (simulator uses Mac CPU/GPU with zero thermal constraints, invalidating metrics).',
    ],
    followUps: [
      {
        id: 'interview.performance.time_profiler.005.f1',
        parentQuestionId: 'interview.performance.time_profiler.005',
        question: 'What is the difference between an Commit Hitch and a Render Hitch in Core Animation?',
        modelAnswer: 'A Commit Hitch occurs when the main thread takes too long to execute layout/draw passes before sending the layer tree to the render server. A Render Hitch occurs when the render server/GPU takes too long to composite the frames (e.g. offscreen rendering, heavy shadows).',
        keyPoints: ['Commit Hitch (Main Thread CPU) vs Render Hitch (Render Server/GPU)'],
        difficulty: 'expert',
      },
    ],
    relatedTopics: ['performance-time-profiler', 'uikit-calayer_vs_uiview'],
  },
  {
    id: 'interview.performance.image_downsampling.006',
    question: 'Write or explain the `CGImageSourceCreateThumbnailAtIndex` technique for downsampling images to avoid memory spikes.',
    domainId: 'performance',
    moduleId: 'performance-profiling-mod',
    topicId: 'performance-image-downsampling',
    difficulty: 'advanced',
    type: 'practical',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Performance', 'ImageIO', 'Downsampling', 'Memory'],
    modelAnswer: 'Loading an image via `UIImage(data:)` decodes the full uncompressed bitmap into RAM (e.g. a 4032x3024 photo occupies ~48MB of memory: `width * height * 4 bytes`). Using `ImageIO`: 1) Create an image source: `CGImageSourceCreateWithData(data, [kCGImageSourceShouldCache: false])`. 2) Specify options: `[kCGImageSourceCreateThumbnailFromImageAlways: true, kCGImageSourceShouldCacheImmediately: true, kCGImageSourceThumbnailMaxPixelSize: maxPixelSize]`. 3) Call `CGImageSourceCreateThumbnailAtIndex(source, 0, options)`. This decodes directly to the target thumbnail size, consuming only ~200KB of RAM instead of 48MB.',
    keyPoints: [
      '`UIImage(data:)` decodes full pixel dimensions regardless of display view size.',
      '`CGImageSourceCreateThumbnailAtIndex` decodes directly to target pixel dimensions.',
      'Setting `kCGImageSourceShouldCache: false` avoids caching uncompressed source buffer.',
      'Performs downsampling on background thread before passing to main thread.',
    ],
    commonMistakes: [
      'Resizing images by drawing into a `UIGraphicsImageRenderer` (this still loads the full 48MB bitmap before scaling down!).',
    ],
    followUps: [
      {
        id: 'interview.performance.image_downsampling.006.f1',
        parentQuestionId: 'interview.performance.image_downsampling.006',
        question: 'How do you calculate `maxPixelSize` for a 100x100pt view on a Retina display?',
        modelAnswer: 'Multiply point size by screen scale: `100pt * UIScreen.main.scale (3x) = 300 pixels`.',
        keyPoints: ['Scale factor multiplication', 'Screen scale pixel conversion'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['performance-image-downsampling', 'system-design-image-pipeline'],
  },
  {
    id: 'interview.accessibility.dynamic_type_scaling.007',
    question: 'How do you implement Dynamic Type properly in UIKit and SwiftUI so custom fonts scale cleanly without truncating UI?',
    domainId: 'accessibility',
    moduleId: 'accessibility-voiceover-mod',
    topicId: 'accessibility-dynamic-type',
    difficulty: 'intermediate',
    type: 'practical',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Accessibility', 'Dynamic Type', 'UIFontMetrics', 'Layout'],
    modelAnswer: 'In UIKit: Use `UIFontMetrics.default.scaledFont(for: customFont)` and set `label.adjustsFontForContentSizeCategory = true`. Use Auto Layout with vertical compression resistance, `numberOfLines = 0`, and avoid hardcoded view heights. In SwiftUI: Use custom fonts with relative text styles: `.font(.custom("FontName", size: 16, relativeTo: .body))`. For layouts, conditionally convert horizontal `HStack`s to vertical `VStack`s using `ViewThatFits` or reading `@Environment(\\.dynamicTypeSize)` when users select Accessibility sizes (AX1-AX5).',
    keyPoints: [
      '`UIFontMetrics` scales custom fonts relative to text styles.',
      '`adjustsFontForContentSizeCategory = true` updates font without reloading controller.',
      '`numberOfLines = 0` and flexible vertical constraints prevent truncation.',
      'Reflow horizontal stacks to vertical stacks at large accessibility text sizes.',
    ],
    commonMistakes: [
      'Setting fixed height constraints (e.g. `heightAnchor.constraint(equalToConstant: 40)`) on buttons or table cells containing text.',
    ],
    followUps: [
      {
        id: 'interview.accessibility.dynamic_type_scaling.007.f1',
        parentQuestionId: 'interview.accessibility.dynamic_type_scaling.007',
        question: 'How does `ViewThatFits` simplify Dynamic Type adaptation in SwiftUI?',
        modelAnswer: '`ViewThatFits` evaluates its children in order and renders the first child that fits within the available space. You can provide an `HStack` first and a `VStack` second; when text enlarges and overflows, it automatically falls back to the `VStack`.',
        keyPoints: ['ViewThatFits fallback', 'Zero manual size category checking'],
        difficulty: 'advanced',
      },
    ],
    relatedTopics: ['accessibility-dynamic-type', 'swiftui-layout-algorithm'],
  },
  {
    id: 'interview.accessibility.voiceover_traits.008',
    question: 'How do you design a custom UI element to be fully accessible with VoiceOver, including traits, values, and custom actions?',
    domainId: 'accessibility',
    moduleId: 'accessibility-voiceover-mod',
    topicId: 'accessibility-voiceover-traits',
    difficulty: 'intermediate',
    type: 'practical',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Accessibility', 'VoiceOver', 'Accessibility Traits', 'Custom Actions'],
    modelAnswer: '1) `isAccessibilityElement = true`: reveals the element to assistive technologies. 2) `accessibilityLabel`: concise description of what the control is (e.g. "Volume"). 3) `accessibilityValue`: current dynamic state (e.g. "80%"). 4) `accessibilityTraits`: specifies behavior (e.g. `.button`, `.adjustable`, `.selected`). 5) `accessibilityHint`: optional description of result of action (e.g. "Double-tap to mute"). 6) `accessibilityCustomActions`: expose secondary operations (e.g. "Delete item", "Archive") to the VoiceOver rotor without cluttering the screen.',
    keyPoints: [
      'Label (noun/what it is) vs Hint (what happens if tapped).',
      'Value represents dynamic state.',
      'Traits define control role (`.button`, `.header`, `.selected`).',
      'Custom actions provide clean rotor actions for swipe-to-delete or popup menus.',
    ],
    commonMistakes: [
      'Including the word "button" in the `accessibilityLabel` (e.g. "Play Button") — VoiceOver automatically reads the trait as "Play, button".',
    ],
    followUps: [
      {
        id: 'interview.accessibility.voiceover_traits.008.f1',
        parentQuestionId: 'interview.accessibility.voiceover_traits.008',
        question: 'What is `accessibilityElementsHidden` vs `accessibilityViewIsModal`?',
        modelAnswer: '`accessibilityElementsHidden = true` hides all subviews from VoiceOver. `accessibilityViewIsModal = true` tells VoiceOver to ignore all other sibling views on screen (crucial for custom alert popups or modal sheets).',
        keyPoints: ['Modal containment for VoiceOver', 'Screen reader focus isolation'],
        difficulty: 'advanced',
      },
    ],
    relatedTopics: ['accessibility-voiceover-traits'],
  },
];
