import type { InterviewQuestion } from '@/types/interview';

export const networkingQuestions: InterviewQuestion[] = [
  {
    id: 'interview.networking.token_refresh_race.001',
    question: 'How do you architect a thread-safe OAuth2 Token Refresh mechanism in Swift to prevent multiple concurrent 401 requests from triggering duplicate refresh calls?',
    domainId: 'networking',
    moduleId: 'networking-production-mod',
    topicId: 'networking-token-refresh',
    difficulty: 'advanced',
    type: 'architecture',
    estimatedMinutes: 5,
    frequency: 'high',
    tags: ['Networking', 'OAuth2', 'Token Refresh', 'Actors', 'Thread Safety'],
    modelAnswer: 'When multiple parallel network requests fail with 401 Unauthorized simultaneously, firing a token refresh for each request creates a "refresh storm" that revokes refresh tokens. Solution: Use a Swift `actor TokenManager` that holds an optional `refreshTask: Task<String, Error>?`. When a 401 occurs, the request calls `tokenManager.refreshToken()`. If `refreshTask` already exists, subsequent callers simply `await refreshTask.value`. If no task exists, a new `Task` is created to call the refresh endpoint. Once complete, `refreshTask` is set back to nil, and all waiting requests resume with the fresh token and replay their requests.',
    keyPoints: [
      'Actor serializes access to token refresh state.',
      'Deduplicate refresh calls by sharing a single in-flight `Task<String, Error>`.',
      'Concurrent 401 callers await the existing shared task instead of spawning new HTTP requests.',
      'Clear refreshTask reference after completion so subsequent failures trigger fresh calls.',
      'Replay original failed requests with updated Authorization header.',
    ],
    commonMistakes: [
      'Using an unprotected boolean `isRefreshing` variable across multiple concurrent threads, causing race conditions.',
    ],
    followUps: [
      {
        id: 'interview.networking.token_refresh_race.001.f1',
        parentQuestionId: 'interview.networking.token_refresh_race.001',
        question: 'What happens if the token refresh call itself fails with 401 or invalid grant?',
        modelAnswer: 'The refresh token is expired or revoked; all queued requests must fail immediately, stored user credentials should be purged from Keychain, and the user must be routed to the login screen.',
        keyPoints: ['Terminal refresh failure', 'Keychain credential purge', 'Force logout'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['networking-token-refresh', 'concurrency-actors'],
  },
  {
    id: 'interview.networking.ssl_pinning.002',
    question: 'How do you implement Certificate / Public Key Pinning using `URLSessionDelegate`, and why is Public Key Pinning preferred over Certificate Pinning?',
    domainId: 'networking',
    moduleId: 'networking-production-mod',
    topicId: 'networking-ssl-pinning',
    difficulty: 'expert',
    type: 'practical',
    estimatedMinutes: 5,
    frequency: 'high',
    tags: ['Networking', 'Security', 'SSL Pinning', 'MITM'],
    modelAnswer: 'Implement `urlSession(_:didReceive:completionHandler:)` for `NSURLAuthenticationMethodServerTrust`. Retrieve the server trust: `challenge.protectionSpace.serverTrust`. Validate the trust evaluation: `SecTrustEvaluateWithError`. Extract the server certificate\'s Subject Public Key Info (SPKI) or public key data using `SecTrustCopyKey(trust)`. Compare the SHA-256 hash of the extracted public key against pre-bundled hardcoded hashes. Public Key Pinning is preferred over Certificate Pinning because certificates expire every 1-2 years (requiring mandatory emergency app updates when renewed), whereas public keys can be preserved across certificate renewals using the same Private Key / CSR.',
    keyPoints: [
      '`URLSessionDelegate` challenge handler verifies server trust before data transfer.',
      'Public Key Pinning (SPKI) survives certificate expiration and renewal.',
      'Certificate Pinning breaks the app when the certificate expires unless updated in advance.',
      'Always configure backup pin hashes (backup keys) to prevent bricking the app during key rotation.',
    ],
    commonMistakes: [
      'Pinning a leaf certificate without including backup pins, causing the app to fail all network calls when the backend certificate is rotated.',
    ],
    followUps: [
      {
        id: 'interview.networking.ssl_pinning.002.f1',
        parentQuestionId: 'interview.networking.ssl_pinning.002',
        question: 'Does SSL pinning protect against a compromised client device with jailbreak tools?',
        modelAnswer: 'No! Tools like Frida or SSL Kill Switch can hook `SecTrustEvaluateWithError` or swizzle the delegate to bypass pinning in memory. Pinning protects against network-level Man-in-the-Middle (MITM) attacks (e.g. malicious Wi-Fi proxies or rogue CAs), not compromised local clients.',
        keyPoints: ['Network MITM defense', 'Does not stop runtime memory hooks on jailbroken devices'],
        difficulty: 'expert',
      },
    ],
    relatedTopics: ['networking-ssl-pinning', 'security-privacy-manifests'],
  },
  {
    id: 'interview.networking.urlcache_etags.003',
    question: 'How does HTTP caching work in iOS using `URLCache`, `ETags`, and conditional GET requests?',
    domainId: 'networking',
    moduleId: 'networking-urlsession-mod',
    topicId: 'networking-caching-urlcache',
    difficulty: 'intermediate',
    type: 'conceptual',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Networking', 'URLCache', 'HTTP Caching', 'ETags'],
    modelAnswer: '`URLCache.shared` provides an automatic in-memory and on-disk HTTP cache. When a server returns an `ETag` (entity tag hash) or `Last-Modified` header, `URLSession` caches the response according to `Cache-Control` directives. On subsequent requests, `URLSession` automatically attaches `If-None-Match: <etag>` or `If-Modified-Since`. If the resource hasn’t changed, the server returns `304 Not Modified` with zero body bytes. `URLSession` intercepts the 304 and returns the cached body data to the client, drastically saving bandwidth and battery.',
    keyPoints: [
      'URLCache handles disk and memory storage transparently.',
      '`Cache-Control` (`max-age`, `no-cache`, `must-revalidate`) directs caching rules.',
      '`If-None-Match` sends ETag back to server for conditional validation.',
      '`304 Not Modified` triggers automatic cache resolution by URLSession.',
    ],
    commonMistakes: [
      'Using `request.cachePolicy = .reloadIgnoringLocalCacheData` unconditionally, defeating all bandwidth savings.',
    ],
    followUps: [
      {
        id: 'interview.networking.urlcache_etags.003.f1',
        parentQuestionId: 'interview.networking.urlcache_etags.003',
        question: 'What is the difference between `no-cache` and `no-store` in HTTP Cache-Control?',
        modelAnswer: '`no-store` instructs the client to NEVER write the response to disk or memory (for sensitive banking/auth data). `no-cache` allows caching the response, but requires validating with the server via conditional GET (ETag) before serving it.',
        keyPoints: ['no-store prevents all caching', 'no-cache allows cache but requires revalidation'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['networking-caching-urlcache', 'networking-urlsession-async'],
  },
  {
    id: 'interview.networking.exponential_backoff.004',
    question: 'What is Exponential Backoff with Jitter, and why is adding random jitter crucial for mobile apps?',
    domainId: 'networking',
    moduleId: 'networking-production-mod',
    topicId: 'networking-token-refresh',
    difficulty: 'intermediate',
    type: 'conceptual',
    estimatedMinutes: 4,
    frequency: 'medium',
    tags: ['Networking', 'Retries', 'Exponential Backoff', 'Jitter'],
    modelAnswer: 'Exponential backoff multiplies the wait time between failed request retries by a factor of 2 (e.g. 1s, 2s, 4s, 8s...) up to a maximum ceiling. If a backend outage affects millions of devices, all devices failing at the same time will retry at identical synchronized intervals (e.g. exactly at 1s, 2s, 4s), hammering the recovering server with synchronized traffic waves (the "Thundering Herd" problem). Adding Jitter (a random variance, e.g. `backoff = base * (2^attempt) + random(0, 1)`) spreads retries smoothly over time, allowing the backend to recover.',
    keyPoints: [
      'Exponential progression: `delay = min(maxDelay, baseDelay * 2^attempt)`.',
      'Prevents hammering struggling backend servers during service outages.',
      'Jitter adds randomized variance to break synchronized client retry spikes.',
      'Solves the Thundering Herd / stampeding herd problem.',
    ],
    commonMistakes: [
      'Retrying non-idempotent requests (like POST payment transactions) blindly on network errors.',
    ],
    followUps: [
      {
        id: 'interview.networking.exponential_backoff.004.f1',
        parentQuestionId: 'interview.networking.exponential_backoff.004',
        question: 'Which HTTP status codes should you retry versus NOT retry?',
        modelAnswer: 'Retry: 503 (Service Unavailable), 504 (Gateway Timeout), 429 (Too Many Requests - check Retry-After header), network timeouts. Do NOT retry: 400 (Bad Request), 401 (needs auth), 403 (Forbidden), 404 (Not Found).',
        keyPoints: ['Transient 5xx/429 errors are retryable', 'Permanent 4xx errors should fail immediately'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['networking-token-refresh', 'system-design-offline-sync'],
  },
  {
    id: 'interview.networking.nwpathmonitor_connectivity.005',
    question: 'How do you monitor real-time network connectivity using `NWPathMonitor`, and what are `isExpensive` and `isConstrained`?',
    domainId: 'networking',
    moduleId: 'networking-production-mod',
    topicId: 'networking-nwpathmonitor',
    difficulty: 'intermediate',
    type: 'practical',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Networking', 'NWPathMonitor', 'Network Framework', 'Low Data Mode'],
    modelAnswer: '`NWPathMonitor` from Apple\'s `Network` framework monitors connectivity changes without polling. Create a monitor (`NWPathMonitor()`), assign a path update handler (`pathUpdateHandler = { path in ... }`), and start on a background dispatch queue. `path.status == .satisfied` indicates active connectivity. `path.isExpensive` is true when using cellular data or personal hotspots. `path.isConstrained` (iOS 13+) is true when the user has enabled "Low Data Mode" in iOS settings. When `isConstrained` or `isExpensive` is true, apps should pause background prefetching, reduce video streaming bitrate, and disable auto-play.',
    keyPoints: [
      'Replaces deprecated reachability libraries with modern Apple Network framework.',
      'Runs on background queue via `start(queue:)`.',
      '`isExpensive`: Cellular data, metered Wi-Fi, or personal hotspots.',
      '`isConstrained`: Low Data Mode enabled by user.',
      'Respect Apple guidelines by deferring high-bandwidth tasks when constrained.',
    ],
    commonMistakes: [
      'Using network connectivity checks to prevent initiating requests (Apple explicitly advises against checking connectivity before firing requests; always fire the request and handle errors).',
    ],
    followUps: [
      {
        id: 'interview.networking.nwpathmonitor_connectivity.005.f1',
        parentQuestionId: 'interview.networking.nwpathmonitor_connectivity.005',
        question: 'Why does Apple recommend attempting the network request instead of blocking on `path.status`?',
        modelAnswer: 'Because interfaces can change state instantaneously (e.g. Wi-Fi waking up or VPN negotiating upon socket demand). Pre-checking reachability often produces false negatives.',
        keyPoints: ['Pre-flight reachability checks produce false negatives', 'Attempt connection directly'],
        difficulty: 'advanced',
      },
    ],
    relatedTopics: ['networking-nwpathmonitor'],
  },
  {
    id: 'interview.networking.background_urlsession.006',
    question: 'How do Background URLSession transfers operate, and how does the app handle wake-ups when transfers complete while suspended?',
    domainId: 'background',
    moduleId: 'background-tasks-mod',
    topicId: 'background-urlsession-transfers',
    difficulty: 'advanced',
    type: 'architecture',
    estimatedMinutes: 5,
    frequency: 'high',
    tags: ['Networking', 'Background URLSession', 'Daemon', 'Lifecycle'],
    modelAnswer: 'Background transfers use `URLSessionConfiguration.background(withIdentifier: "...")`. When initiated, transfer management is delegated to an out-of-process system daemon (`nsurlsessiond`). The transfer continues even if the app process is suspended or terminated by Jetsam. When all tasks complete or fail, the system launches the app into the background and calls `application(_:handleEventsForBackgroundURLSession:completionHandler:)` in AppDelegate. The app re-instantiates the background session with the same identifier, hooks delegates to receive completed file data, and calls the system completion handler in `urlSessionDidFinishEvents(forBackgroundURLSession:)`.',
    keyPoints: [
      'Delegates network transfer to system daemon `nsurlsessiond`.',
      'Survives application suspension and termination.',
      '`handleEventsForBackgroundURLSession` wakes app up in background.',
      'Must re-create URLSession with identical identifier string.',
      'Must execute completionHandler in `urlSessionDidFinishEvents` to allow system to take UI snapshot and re-suspend.',
    ],
    commonMistakes: [
      'Forgetting to call the saved completion handler in `urlSessionDidFinishEvents`, leading to system app termination warnings for holding background assertion.',
    ],
    followUps: [
      {
        id: 'interview.networking.background_urlsession.006.f1',
        parentQuestionId: 'interview.networking.background_urlsession.006',
        question: 'Can you use closure-based async/await data(for:) APIs with a background URLSession?',
        modelAnswer: 'No! Background URLSessions require traditional delegate-based architectures because asynchronous closures cannot survive application process termination.',
        keyPoints: ['Delegate required for background sessions', 'Async closures cannot be resumed after termination'],
        difficulty: 'advanced',
      },
    ],
    relatedTopics: ['background-urlsession-transfers', 'app-lifecycle-transitions'],
  },
  {
    id: 'interview.networking.websockets_lifecycle.007',
    question: 'How do you implement full-duplex communication using `URLSessionWebSocketTask` with heartbeat ping/pong and reconnection?',
    domainId: 'networking',
    moduleId: 'networking-production-mod',
    topicId: 'networking-websockets',
    difficulty: 'advanced',
    type: 'practical',
    estimatedMinutes: 5,
    frequency: 'medium',
    tags: ['Networking', 'WebSockets', 'Real-Time', 'Heartbeat'],
    modelAnswer: '1) Create task: `let task = urlSession.webSocketTask(with: url); task.resume()`. 2) Receiving messages: WebSockets receive messages one at a time. Create a recursive receive function: `task.receive { [weak self] result in switch result { case .success(let message): self?.handle(message); self?.listen() case .failure(let error): self?.handleDisconnect(error) } }`. 3) Heartbeats: Ping the server every 30s using `task.sendPing { error in ... }` to keep intermediate proxies and cellular NAT gateways from closing the idle TCP connection. 4) Reconnect: On socket failure or app foregrounding, schedule an exponential backoff reconnect.',
    keyPoints: [
      '`webSocketTask(with:)` provides native WebSocket support.',
      'Recursive `receive()` pattern is required (messages are delivered one by one).',
      'Periodic `sendPing` prevents cellular NAT timeout teardowns.',
      'Handle app lifecycle (pause socket in background, reconnect on foreground).',
    ],
    commonMistakes: [
      'Calling `task.receive()` only once and expecting all future messages to trigger it like a continuous stream.',
    ],
    followUps: [
      {
        id: 'interview.networking.websockets_lifecycle.007.f1',
        parentQuestionId: 'interview.networking.websockets_lifecycle.007',
        question: 'Can you bridge `URLSessionWebSocketTask` to an `AsyncSequence`?',
        modelAnswer: 'Yes, by wrapping the recursive receive loop inside an `AsyncStream<URLSessionWebSocketTask.Message>`, yielding messages as they arrive and finishing on cancellation.',
        keyPoints: ['AsyncStream wrapper', 'Idiomatic Swift Concurrency streaming'],
        difficulty: 'advanced',
      },
    ],
    relatedTopics: ['networking-websockets', 'concurrency-async-await'],
  },
  {
    id: 'interview.networking.http2_vs_http1.008',
    question: 'How does HTTP/2 Multiplexing improve mobile app network latency compared to HTTP/1.1?',
    domainId: 'networking',
    moduleId: 'networking-urlsession-mod',
    topicId: 'networking-urlsession-async',
    difficulty: 'intermediate',
    type: 'conceptual',
    estimatedMinutes: 4,
    frequency: 'medium',
    tags: ['Networking', 'HTTP/2', 'Multiplexing', 'TCP'],
    modelAnswer: 'In HTTP/1.1, each TCP connection can only handle one request-response exchange at a time (Head-of-Line blocking). Browsers and apps had to open 4-6 parallel TCP sockets, causing high TCP handshake and TLS negotiation overhead on mobile cellular radios. In HTTP/2, multiple independent requests and responses are interleaved concurrently as binary frames over a single shared TCP connection (Multiplexing). Furthermore, HTTP/2 HPACK header compression drastically compresses repetitive HTTP request headers (like authorization cookies and user agents), saving packet bandwidth.',
    keyPoints: [
      'HTTP/1.1 suffered from Head-of-Line blocking (one request per socket).',
      'HTTP/2 multiplexes multiple bidirectional streams over a single TCP connection.',
      'HPACK header compression saves cellular radio upload bytes.',
      '`URLSession` enables HTTP/2 and HTTP/3 automatically by default.',
    ],
    commonMistakes: [
      'Assuming you must manually configure HTTP/2 in iOS (URLSession negotiates it via ALPN during TLS handshake).',
    ],
    followUps: [
      {
        id: 'interview.networking.http2_vs_http1.008.f1',
        parentQuestionId: 'interview.networking.http2_vs_http1.008',
        question: 'What remaining Head-of-Line blocking exists in HTTP/2 that HTTP/3 (QUIC) solves?',
        modelAnswer: 'In HTTP/2, because all streams share one TCP connection, a single dropped TCP packet halts delivery of all multiplexed streams until retransmitted. HTTP/3 over QUIC uses UDP, so dropped packets in one stream do not block unrelated streams.',
        keyPoints: ['TCP packet loss blocks all streams in HTTP/2', 'QUIC (UDP) isolates packet loss to individual streams'],
        difficulty: 'expert',
      },
    ],
    relatedTopics: ['networking-urlsession-async'],
  },
  {
    id: 'interview.networking.codable_custom_dates.009',
    question: 'How do you decode non-standard or heterogeneous date formats within a single JSON payload using `Codable`?',
    domainId: 'foundation',
    moduleId: 'foundation-codable-mod',
    topicId: 'foundation-codable-deep-dive',
    difficulty: 'intermediate',
    type: 'practical',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Foundation', 'Codable', 'JSONDecoder', 'Dates'],
    modelAnswer: 'If all dates in a payload share a format, set `decoder.dateDecodingStrategy = .iso8601` or `.formatted(customDateFormatter)`. However, if different fields have heterogeneous date formats (e.g. one is UNIX timestamp epoch integer, another is ISO 8601 string, another is "yyyy-MM-dd"), configure `decoder.dateDecodingStrategy = .custom { decoder in ... }` to inspect the container dynamically, OR create a custom property wrapper `@CustomDate<Strategy>` that handles decoding per property, OR implement manual `init(from: Decoder)` on the model.',
    keyPoints: [
      '`dateDecodingStrategy.custom` allows programmatic date resolution.',
      'Property wrappers (`@ISO8601Date`, `@EpochDate`) allow per-field decoding strategies.',
      'ISO8601DateFormatter vs DateFormatter caching (DateFormatter allocation is expensive).',
    ],
    commonMistakes: [
      'Allocating a new `DateFormatter` on every decoded element in a tight loop (DateFormatter is slow to allocate; reuse a static or thread-local instance).',
    ],
    followUps: [
      {
        id: 'interview.networking.codable_custom_dates.009.f1',
        parentQuestionId: 'interview.networking.codable_custom_dates.009',
        question: 'Why is `DateFormatter` not thread-safe in early iOS, and how does `ISO8601DateFormatter` compare?',
        modelAnswer: '`DateFormatter` historically had internal mutations during formatting, causing crashes when accessed across threads. `ISO8601DateFormatter` and modern iOS 15 `.formatted()` APIs are thread-safe and faster.',
        keyPoints: ['DateFormatter thread-safety risks', 'Modern thread-safe formatted() APIs'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['foundation-codable-deep-dive', 'foundation-date-parsing'],
  },
  {
    id: 'interview.networking.network_mocking_tests.010',
    question: 'How do you mock network requests cleanly in unit tests without third-party frameworks like OHHTTPStubs or Moya?',
    domainId: 'testing',
    moduleId: 'testing-unit-mod',
    topicId: 'testing-test-doubles-mocking',
    difficulty: 'advanced',
    type: 'practical',
    estimatedMinutes: 5,
    frequency: 'high',
    tags: ['Testing', 'Networking', 'URLProtocol', 'Mocking'],
    modelAnswer: 'Use Apple’s built-in `URLProtocol` subclass! 1) Create `MockURLProtocol: URLProtocol`. In `canInit(with:)`, return `true` to intercept requests. 2) In `startLoading()`, return mock `HTTPURLResponse` and `Data` or call `client?.urlProtocol(self, didFailWithError:)`. 3) Configure a custom ephemeral `URLSessionConfiguration`: `config.protocolClasses = [MockURLProtocol.self]`. 4) Inject this `URLSession(configuration: config)` into your API client. This tests the entire networking stack (URLRequest construction, HTTP header serialization, JSON decoding, status code handling) with zero network calls.',
    keyPoints: [
      '`URLProtocol` intercepts network calls at the foundation layer.',
      'Tests full stack including request creation, headers, and Codable parsing.',
      'Requires zero external dependencies or swizzling.',
      'Use ephemeral `URLSessionConfiguration.protocolClasses` to isolate test sessions.',
    ],
    commonMistakes: [
      'Registering MockURLProtocol globally via `URLProtocol.registerClass()`, which pollutes parallel test runs across the test suite.',
    ],
    followUps: [
      {
        id: 'interview.networking.network_mocking_tests.010.f1',
        parentQuestionId: 'interview.networking.network_mocking_tests.010',
        question: 'How does URLProtocol mocking compare to protocol-based API client mocking (`interface APIClient`)?',
        modelAnswer: 'Protocol mocking verifies what the ViewModel calls, but does NOT test your URLSession decoding, header generation, or JSON serialization. URLProtocol tests the actual networking client code without hitting the network.',
        keyPoints: ['Tests URLRequest building and JSON parsing', 'More comprehensive integration test'],
        difficulty: 'advanced',
      },
    ],
    relatedTopics: ['testing-test-doubles-mocking', 'networking-urlsession-async'],
  },
];
