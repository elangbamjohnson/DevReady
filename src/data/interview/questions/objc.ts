import type { InterviewQuestion } from '@/types/interview';

export const objcQuestions: InterviewQuestion[] = [
  {
    id: 'interview.objc.msgsend_pipeline.001',
    question: 'How does Objective-C message dispatch (`objc_msgSend`) work, and what are the 3 stages of Message Forwarding?',
    domainId: 'objc',
    moduleId: 'objc-runtime-mod',
    topicId: 'objc-msgsend-dispatch',
    difficulty: 'advanced',
    type: 'conceptual',
    estimatedMinutes: 5,
    frequency: 'high',
    tags: ['Objective-C', 'Runtime', 'objc_msgSend', 'Message Forwarding'],
    modelAnswer: 'In Objective-C, method calls are dynamic messages: `[receiver message]` compiles to `objc_msgSend(receiver, selector, ...)`. The runtime follows receiver\'s `isa` pointer to its class, checks the method cache (fast path), then searches the class method list and superclass chain. If no implementation (`IMP`) is found, Message Forwarding begins: 1) Method Resolution: calls `resolveInstanceMethod:` / `resolveClassMethod:`, giving the class a chance to add an IMP dynamically via `class_addMethod`. 2) Fast Forwarding: calls `forwardingTargetForSelector:` to forward the message to a different object. 3) Normal Forwarding: creates an `NSInvocation` via `methodSignatureForSelector:` and invokes `forwardInvocation:`. If all 3 fail, `doesNotRecognizeSelector:` crashes the app.',
    keyPoints: [
      'objc_msgSend follows `isa` pointer and inspects method cache first.',
      'Stage 1: Method Resolution (`resolveInstanceMethod:`) dynamically adds IMP.',
      'Stage 2: Fast Forwarding (`forwardingTargetForSelector:`) delegates to another target.',
      'Stage 3: Normal Forwarding (`forwardInvocation:`) packages NSInvocation.',
      'Crash: `doesNotRecognizeSelector:` (unrecognized selector sent to instance).',
    ],
    commonMistakes: [
      'Assuming Objective-C method calls use static v-tables (they are pure dynamic message sends).',
    ],
    followUps: [
      {
        id: 'interview.objc.msgsend_pipeline.001.f1',
        parentQuestionId: 'interview.objc.msgsend_pipeline.001',
        question: 'What is the performance difference between Fast Forwarding and Normal Forwarding?',
        modelAnswer: 'Fast forwarding (`forwardingTargetForSelector:`) simply returns another target object with zero heap allocation. Normal forwarding allocates a heavy `NSInvocation` object on the heap containing all boxed arguments, which is much slower.',
        keyPoints: ['NSInvocation heap allocation overhead', 'Fast forwarding avoids boxing'],
        difficulty: 'advanced',
      },
    ],
    relatedTopics: ['objc-msgsend-dispatch', 'objc-forwarding'],
  },
  {
    id: 'interview.objc.method_swizzling.002',
    question: 'What is Method Swizzling in Objective-C? How do you implement it safely, and what are the main dangers?',
    domainId: 'objc',
    moduleId: 'objc-runtime-mod',
    topicId: 'objc-method-swizzling',
    difficulty: 'expert',
    type: 'practical',
    estimatedMinutes: 5,
    frequency: 'high',
    tags: ['Objective-C', 'Runtime', 'Method Swizzling', 'Analytics'],
    modelAnswer: 'Method Swizzling exchanges the implementation (`IMP`) of two selectors at runtime using `method_exchangeImplementations`. Commonly used for analytics (e.g. tracking screen appearances by swizzling `viewWillAppear:`). Safe implementation: 1) Swizzle inside `+load` (or a dedicated static setup) inside `dispatch_once`. 2) Use `class_addMethod` first: if the class inherits the method from a superclass, exchanging implementations without adding it to the subclass first will inadvertently swizzle the superclass implementation, corrupting all other subclasses! 3) Always call the original implementation inside the swizzled method to preserve system behavior.',
    keyPoints: [
      'Exchanges method IMP pointers at runtime.',
      'Must use `class_addMethod` first to avoid corrupting superclass implementations.',
      'Always call the original implementation (via the swizzled selector) to prevent breaking system contracts.',
      'Dangers: Naming collisions, unpredictable execution order when multiple SDKs swizzle the same method, debugging nightmares.',
    ],
    commonMistakes: [
      'Forgetting to call the original implementation, breaking fundamental UIKit view lifecycles.',
      'Calling `class_getInstanceMethod` and `method_exchangeImplementations` without `class_addMethod`.',
    ],
    followUps: [
      {
        id: 'interview.objc.method_swizzling.002.f1',
        parentQuestionId: 'interview.objc.method_swizzling.002',
        question: 'Can you swizzle pure Swift methods?',
        modelAnswer: 'No, unless the method belongs to an `NSObject` subclass and is explicitly annotated with `dynamic` (or `@objc dynamic`), which forces Objective-C message dispatch.',
        keyPoints: ['Requires dynamic keyword', 'NSObject subclass required'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['objc-method-swizzling', 'swift-dispatch-witness-tables'],
  },
  {
    id: 'interview.objc.property_attributes_copy.003',
    question: 'Why should `NSString`, `NSArray`, and block properties always use the `copy` attribute instead of `strong` in Objective-C?',
    domainId: 'objc',
    moduleId: 'objc-core-mod',
    topicId: 'objc-properties-memory',
    difficulty: 'intermediate',
    type: 'conceptual',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Objective-C', 'Properties', 'copy', 'Memory Attributes'],
    modelAnswer: '`NSString` and `NSArray` have mutable subclasses: `NSMutableString` and `NSMutableArray`. If a property is declared with `strong`, a caller can pass an `NSMutableString`. When the caller subsequently mutates that string elsewhere, the property value changes under the hood without the owning class\'s knowledge, violating encapsulation. `copy` invokes `[obj copy]`, creating an immutable snapshot if the incoming object is mutable. For blocks: blocks start on the stack; `copy` moves the block from the stack to the heap so it can safely outlive the creating scope.',
    keyPoints: [
      '`NSMutableString` can be passed to an `NSString` parameter.',
      '`strong` retains the mutable reference; external mutations alter internal state.',
      '`copy` guarantees an immutable defensive copy is created.',
      'Blocks are moved from stack to heap via `copy`.',
    ],
    commonMistakes: [
      'Using `strong` for block or NSString properties in Objective-C.',
    ],
    followUps: [
      {
        id: 'interview.objc.property_attributes_copy.003.f1',
        parentQuestionId: 'interview.objc.property_attributes_copy.003',
        question: 'What happens when `[NSString copy]` is called on an already immutable NSString?',
        modelAnswer: 'It simply increments the reference count and returns `self` (no new memory is allocated). Copying is only an allocation cost when the object is an `NSMutableString`.',
        keyPoints: ['Immutable copy returns self', 'Zero allocation overhead on immutable instances'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['objc-properties-memory', 'objc-blocks'],
  },
  {
    id: 'interview.objc.kvo_isa_swizzling.004',
    question: 'How is Key-Value Observing (KVO) implemented by the Objective-C runtime? What is `isa`-swizzling?',
    domainId: 'objc',
    moduleId: 'objc-runtime-mod',
    topicId: 'objc-kvc-kvo',
    difficulty: 'advanced',
    type: 'conceptual',
    estimatedMinutes: 5,
    frequency: 'high',
    tags: ['Objective-C', 'KVO', 'isa-swizzling', 'Runtime'],
    modelAnswer: 'When an observer registers via `addObserver:forKeyPath:options:context:`, the runtime uses `isa`-swizzling: 1) It dynamically generates a new subclass at runtime prefixed with `NSKVONotifying_OriginalClass`. 2) It swizzles the target object\'s `isa` pointer to point to this dynamic subclass. 3) In the subclass, it overrides the property setter (`setName:`) to wrap calls in `willChangeValueForKey:` and `didChangeValueForKey:`. 4) It also overrides the `class` method to return `OriginalClass` rather than `NSKVONotifying_OriginalClass`, hiding the dynamic subclass from public introspection.',
    keyPoints: [
      'Runtime generates `NSKVONotifying_ClassName` subclass on the fly.',
      'Object\'s `isa` pointer is changed to point to the KVO subclass.',
      'Setter is overridden with `willChangeValueForKey:` and `didChangeValueForKey:`.',
      'The `class` method is swizzled to disguise the object as its original class.',
    ],
    commonMistakes: [
      'Checking class type with `object_getClass(obj)` vs `[obj class]` (object_getClass reveals NSKVONotifying; [obj class] is disguised).',
    ],
    followUps: [
      {
        id: 'interview.objc.kvo_isa_swizzling.004.f1',
        parentQuestionId: 'interview.objc.kvo_isa_swizzling.004',
        question: 'Why should you always pass a unique static context pointer in `addObserver:forKeyPath:options:context:`?',
        modelAnswer: 'To prevent ambiguity when both a superclass and a subclass observe the same keypath on the same target. Checking `context == &MyContext` ensures you only handle notifications intended for your class.',
        keyPoints: ['Observer context pointer disambiguation', 'Prevents superclass collision'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['objc-kvc-kvo', 'objc-msgsend-dispatch'],
  },
  {
    id: 'interview.objc.blocks_weak_dance.005',
    question: 'Explain the difference between `__block` and `__weak` in Objective-C block memory management. What is the `weakSelf`/`strongSelf` dance?',
    domainId: 'objc',
    moduleId: 'objc-core-mod',
    topicId: 'objc-blocks',
    difficulty: 'intermediate',
    type: 'code-analysis',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Objective-C', 'Blocks', '__weak', '__block', 'Retain Cycles'],
    modelAnswer: '`__weak` creates a non-owning zeroing reference that prevents retain cycles. `__block` allows a variable to be mutated from inside the block by promoting it from the stack to a heap-allocated `byref` structure (it does NOT prevent retain cycles). The `weakSelf`/`strongSelf` dance: 1) `__weak typeof(self) weakSelf = self;` creates a weak reference captured by the block. 2) Inside the block: `__strong typeof(weakSelf) strongSelf = weakSelf; if (!strongSelf) return;`. This creates a temporary strong reference for the duration of the block execution, guaranteeing `self` is not deallocated halfway through execution.',
    keyPoints: [
      '`__weak` breaks retain cycles by zeroing reference.',
      '`__block` allows in-block variable mutation (promotes variable to heap).',
      '`strongSelf` prevents deallocation in the middle of block execution.',
      'In MRC, `__block` did not retain; in ARC, `__block` DOES retain objects unless combined with `__weak`.',
    ],
    commonMistakes: [
      'Assuming `__block` breaks retain cycles in ARC (in ARC, `__block id obj` strongly retains the object!).',
    ],
    followUps: [
      {
        id: 'interview.objc.blocks_weak_dance.005.f1',
        parentQuestionId: 'interview.objc.blocks_weak_dance.005',
        question: 'What happens if you use `weakSelf` directly inside a block without the `strongSelf` assignment?',
        modelAnswer: '`weakSelf` could become nil between two consecutive lines of code if the object is deallocated concurrently on another thread, leading to half-executed operations.',
        keyPoints: ['Nil mid-execution hazard', 'strongSelf locks reference during block execution'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['objc-blocks', 'memory-retain-cycles'],
  },
  {
    id: 'interview.objc.categories_vs_extensions.006',
    question: 'What are the technical differences between Objective-C Categories and Class Extensions (Anonymous Categories)?',
    domainId: 'objc',
    moduleId: 'objc-core-mod',
    topicId: 'objc-categories-extensions',
    difficulty: 'intermediate',
    type: 'comparison',
    estimatedMinutes: 3,
    frequency: 'medium',
    tags: ['Objective-C', 'Categories', 'Extensions', 'Runtime'],
    modelAnswer: 'Class Extensions (`@interface MyClass ()`) are declared at compile-time in the `.m` implementation file of the class itself. They can add private stored properties and instance variables, and the compiler verifies that all declared methods are implemented. Categories (`@interface MyClass (MyCategory)`) can be added to any class without source access at runtime. Categories CANNOT add stored properties (without Associated Objects), and method names that collide with existing methods overwrite the original implementation at runtime with no compiler warning.',
    keyPoints: [
      'Class Extensions can add stored properties; Categories cannot (without Associated Objects).',
      'Class Extensions require source access and compile with the main class.',
      'Categories are loaded at runtime by the loader.',
      'Category method name collisions override the original method unpredictably.',
    ],
    commonMistakes: [
      'Declaring a stored property in a Category expecting the compiler to synthesize an ivar.',
    ],
    followUps: [
      {
        id: 'interview.objc.categories_vs_extensions.006.f1',
        parentQuestionId: 'interview.objc.categories_vs_extensions.006',
        question: 'How do you attach stored state to a category using the runtime?',
        modelAnswer: 'Using Associated Objects: `objc_setAssociatedObject(self, &key, value, OBJC_ASSOCIATION_RETAIN_NONATOMIC)` and `objc_getAssociatedObject(self, &key)`.',
        keyPoints: ['objc_setAssociatedObject', 'Association policies'],
        difficulty: 'advanced',
      },
    ],
    relatedTopics: ['objc-categories-extensions'],
  },
  {
    id: 'interview.objc.swift_interop_headers.007',
    question: 'How do Bridging Headers (`<Target>-Bridging-Header.h`) and Generated Headers (`<Product>-Swift.h`) work in mixed Objective-C and Swift projects?',
    domainId: 'objc',
    moduleId: 'objc-interop-mod',
    topicId: 'objc-bridging-headers',
    difficulty: 'intermediate',
    type: 'practical',
    estimatedMinutes: 4,
    frequency: 'high',
    tags: ['Objective-C', 'Swift Interop', 'Bridging Header', 'Product-Swift.h'],
    modelAnswer: 'The Bridging Header (`<Target>-Bridging-Header.h`) exposes Objective-C headers to Swift code in an app target. When Swift files compile, the compiler imports everything in the bridging header into the Swift module namespace. In reverse, when Objective-C needs to call Swift, the compiler automatically generates a private header `<Product>-Swift.h`. Only Swift classes that inherit from `NSObject` and methods/properties marked with `@objc` (or classes marked `@objcMembers`) are emitted into `<Product>-Swift.h`. In Swift Frameworks (framework targets), bridging headers are not allowed; you must use the umbrella header and `module.modulemap` instead.',
    keyPoints: [
      'Bridging Header exposes Obj-C to Swift (App targets only).',
      '`<Product>-Swift.h` exposes Swift classes to Obj-C.',
      'Swift types must be `@objc` and inherit from `NSObject` to be visible to Obj-C.',
      'Framework targets require umbrella headers and module maps, not bridging headers.',
    ],
    commonMistakes: [
      'Trying to use a bridging header in an SPM package or dynamic framework target (rejected by compiler; use umbrella headers).',
    ],
    followUps: [
      {
        id: 'interview.objc.swift_interop_headers.007.f1',
        parentQuestionId: 'interview.objc.swift_interop_headers.007',
        question: 'Can pure Swift structs, enums with associated values, or tuples be exposed to Objective-C?',
        modelAnswer: 'No. Objective-C cannot represent Swift value types, generic protocols, or enums with associated values. They cannot be annotated with `@objc`.',
        keyPoints: ['Unsupported Swift primitives in Obj-C', 'Requires class wrappers'],
        difficulty: 'intermediate',
      },
    ],
    relatedTopics: ['objc-bridging-headers', 'objc-annotations-nullability'],
  },
  {
    id: 'interview.objc.nullability_generics.008',
    question: 'How do Nullability annotations (`_Nonnull`, `_Nullable`, `null_resettable`) and lightweight generics improve Swift interoperability?',
    domainId: 'objc',
    moduleId: 'objc-interop-mod',
    topicId: 'objc-annotations-nullability',
    difficulty: 'intermediate',
    type: 'conceptual',
    estimatedMinutes: 3,
    frequency: 'medium',
    tags: ['Objective-C', 'Nullability', 'Generics', 'Swift Interop'],
    modelAnswer: 'Without nullability annotations, all Objective-C pointers imported into Swift become Implicitly Unwrapped Optionals (`Type!`), which are dangerous and cause crashes if nil. Annotating with `nonnull` / `_Nonnull` imports types as non-optional (`Type`), while `nullable` / `_Nullable` imports them as safe Optionals (`Type?`). `null_resettable` indicates a property can be set to nil to reset it to a default non-nil value (e.g. `tintColor`). `NS_ASSUME_NONNULL_BEGIN / END` macros apply nonnull defaults across header files. Lightweight generics (`NSArray<NSString *> *`) translate untyped `[Any]` into strongly typed `[String]` in Swift.',
    keyPoints: [
      'Prevents dangerous Implicitly Unwrapped Optionals (`Type!`) in Swift.',
      '`NS_ASSUME_NONNULL_BEGIN / END` reduces annotation noise.',
      '`null_resettable`: setter accepts nil, getter always returns non-nil.',
      'Lightweight generics map Objective-C collections to typed Swift arrays and dictionaries.',
    ],
    commonMistakes: [
      'Returning nil from an Objective-C method marked `_Nonnull`, causing silent memory corruption or unexpected unwrapping crashes in Swift.',
    ],
    followUps: [
      {
        id: 'interview.objc.nullability_generics.008.f1',
        parentQuestionId: 'interview.objc.nullability_generics.008',
        question: 'What is `__covariant` and `__contravariant` in Objective-C lightweight generics?',
        modelAnswer: 'They control subtyping relationships: `__covariant` means `NSArray<Subclass *>` can be passed where `NSArray<Superclass *>` is expected.',
        keyPoints: ['Subtyping variance', 'Covariant collection subtyping'],
        difficulty: 'advanced',
      },
    ],
    relatedTopics: ['objc-annotations-nullability', 'swift-optionals'],
  },
];
